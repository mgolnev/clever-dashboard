meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile
  skip: false

run:
  # Приложение слушает :8080 (непривилегированный порт; setcap на Amvera не работает).
  containerPort: 8080
  persistenceMount: /data
